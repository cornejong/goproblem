package tests

import (
	"testing"
)

func TestBuilder(t *testing.T) {
	// p := problem.New(
	// 	problem.Title("Title"),
	// 	problem.Status(200),
	// 	problem.Detail("detail"),
	// 	// problem.Extension("", map[string]string{}),
	// )

}
